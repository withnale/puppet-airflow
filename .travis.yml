---
sudo: false
language: ruby
cache: bundler
matrix:
  fast_finish: true
  include:
  - rvm: 2.1.6
    dist: trusty
    env: PUPPET_INSTALL_TYPE=agent BEAKER_debug=true BEAKER_set=docker-ubuntu-1204
    script: bundle exec rake beaker
    services: docker
    sudo: required
notifications:
  email: false

